import { ArrowRight, Moon, ShieldCheck, Sparkles, Sun } from "lucide-react";
import type { Merchant } from "../types";

export function LoginPage({ merchants, onLogin, loading, error, theme, onThemeToggle }: { merchants: Merchant[]; onLogin: (id: number) => void; loading: boolean; error: string | null; theme: "dark" | "light"; onThemeToggle: () => void }) {
  return <main className={`min-h-screen bg-slate-950 px-6 py-12 text-slate-100 ${theme === "light" ? "theme-light" : ""}`}><div className="mx-auto grid max-w-5xl gap-10 lg:grid-cols-[1.1fr_.9fr] lg:items-center"><section><div className="mb-8 flex items-center justify-between gap-3"><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl bg-violet-500"><Sparkles /></div><span className="text-xl font-semibold">Arvya</span></div><button aria-label="Toggle color theme" onClick={onThemeToggle} className="grid h-9 w-9 place-items-center rounded-lg border border-white/10 text-slate-400 hover:bg-white/5 hover:text-white">{theme === "dark" ? <Sun size={17} /> : <Moon size={17} />}</button></div><p className="text-sm font-semibold uppercase tracking-[.22em] text-violet-300">AI Merchant Growth Agent</p><h1 className="mt-4 max-w-xl text-4xl font-semibold leading-tight tracking-tight sm:text-5xl">Create governed offers that convert into commerce.</h1><p className="mt-5 max-w-xl text-lg leading-8 text-slate-400">Start with a seeded merchant workspace. Day 1 establishes clean catalogs, merchant context, and a trusted activity ledger.</p><div className="mt-8 flex gap-6 text-sm text-slate-400"><span className="flex items-center gap-2"><ShieldCheck size={17} className="text-emerald-300" /> Auditable by default</span><span className="flex items-center gap-2"><Sparkles size={17} className="text-violet-300" /> Agent-ready</span></div></section><section className="rounded-3xl border border-white/10 bg-slate-900 p-6 shadow-2xl shadow-black/30"><h2 className="text-lg font-semibold">Choose a demo merchant</h2><p className="mt-2 text-sm text-slate-400">Select skincare, coffee, or fitness catalogs for different growth scenarios.</p><div className="mt-6 space-y-3">{merchants.map((merchant) => <button disabled={loading} key={merchant.id} onClick={() => onLogin(merchant.id)} className="group flex w-full items-center justify-between rounded-xl border border-white/10 bg-white/[.03] p-4 text-left hover:border-violet-400/60 hover:bg-violet-500/10 disabled:opacity-50"><span><span className="block font-medium">{merchant.name}</span><span className="mt-1 block text-xs text-slate-500">{merchant.industry}</span></span><ArrowRight size={18} className="text-slate-500 transition group-hover:translate-x-1 group-hover:text-violet-300" /></button>)}</div>{error && <p className="mt-4 rounded-lg bg-rose-500/10 p-3 text-sm text-rose-300">{error}</p>}</section></div></main>;
}
