import { Activity, BadgeCheck, BarChart3, Bot, CheckCircle2, CircleAlert, ClipboardCheck, IndianRupee, Play, ShieldCheck, Sparkles, Ticket, TrendingUp } from "lucide-react";
import { useEffect, useState } from "react";
import type { CommerceSimulation, DecisionCenter } from "../types";

const money = (paise: number) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(paise / 100);

export function DecisionCenterPage({ decision, simulation, loading, running, error, onRunSimulation }: { decision: DecisionCenter | null; simulation: CommerceSimulation | null; loading: boolean; running: boolean; error: string | null; onRunSimulation: () => Promise<void> }) {
  if (loading) return <section className="grid min-h-[55vh] place-items-center"><p className="text-sm text-slate-400">Preparing governed decision trace…</p></section>;
  if (!decision) return <section className="grid min-h-[55vh] place-items-center rounded-2xl border border-dashed border-violet-400/35 bg-violet-500/5 p-8 text-center"><div><Sparkles className="mx-auto text-violet-200" /><h2 className="mt-4 text-xl font-semibold">No governed decision is ready yet</h2><p className="mt-2 max-w-md text-sm leading-6 text-slate-400">Generate a growth opportunity, then return here to inspect its confidence, evidence, guardrails, and audit trace.</p></div></section>;
  const riskStyle = decision.risk_level === "low" ? "border-emerald-400/25 bg-emerald-400/10 text-emerald-300" : decision.risk_level === "medium" ? "border-amber-400/25 bg-amber-400/10 text-amber-200" : "border-rose-400/25 bg-rose-400/10 text-rose-200";
  return <div className="space-y-7"><section className="relative overflow-hidden rounded-2xl border border-violet-400/25 bg-gradient-to-br from-violet-500/15 via-slate-900 to-slate-950 p-7 shadow-xl shadow-violet-950/20 sm:p-9"><div className="absolute -right-20 -top-20 h-64 w-64 rounded-full bg-cyan-400/10 blur-3xl" /><div className="relative flex flex-wrap items-end justify-between gap-5"><div><p className="text-sm font-semibold uppercase tracking-[.18em] text-violet-200">AI Decision Center</p><h2 className="mt-2 max-w-3xl text-3xl font-semibold tracking-tight">One governed decision. Every reason visible.</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-300">Arvya combines catalog evidence, commercial guardrails, merchant approval and Razorpay-safe execution. AI explains the decision; rules protect the money.</p></div><button onClick={() => void onRunSimulation()} disabled={running} className="inline-flex items-center gap-2 rounded-lg bg-white px-4 py-3 text-sm font-semibold text-slate-950 shadow-sm hover:bg-violet-50 disabled:opacity-60"><Play size={16} fill="currentColor" /> {running ? "Running safe simulation…" : "Run Full Commerce Simulation"}</button></div><div className="relative mt-7 grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><HeaderStat icon={<BadgeCheck size={18} />} label="Approval" value={decision.approval_status.replace("_", " ")} tone="violet" /><HeaderStat icon={<Activity size={18} />} label="Decision confidence" value={`${decision.confidence_score}%`} tone="cyan" /><HeaderStat icon={<ShieldCheck size={18} />} label="Risk score" value={`${decision.risk_score}/100 · ${decision.risk_level}`} tone="emerald" /><HeaderStat icon={<TrendingUp size={18} />} label="Expected monthly lift" value={`₹${decision.expected_monthly_uplift_inr.toLocaleString("en-IN")}`} tone="amber" /></div></section>{error && <div className="rounded-xl border border-rose-400/20 bg-rose-400/10 p-4 text-sm text-rose-200">{error}</div>}<section className="grid gap-6 xl:grid-cols-[1.12fr_.88fr]"><article className="rounded-2xl border border-white/10 bg-slate-900/80 p-6"><div className="flex items-start gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-violet-500/15 text-violet-200"><Bot size={20} /></div><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-violet-200">Selected recommendation</p><h3 className="mt-1 text-xl font-semibold">{decision.title}</h3><p className="mt-1 text-sm text-slate-400">{decision.recommendation_type} · merchant {decision.approval_status.replace("_", " ")}</p></div></div><div className="mt-6 grid gap-3 rounded-xl border border-white/10 bg-white/[.03] p-4 sm:grid-cols-3"><Metric label="Original total" value={money(decision.original_price_paise)} /><Metric label="Merchant offer" value={money(decision.offer_price_paise)} /><Metric label="Final payable" value={money(decision.final_price_paise)} /></div><div className="mt-4 flex flex-wrap gap-2">{decision.coupon_code && <span className="inline-flex items-center gap-1 rounded-full bg-emerald-400/10 px-3 py-1.5 text-xs font-medium text-emerald-300"><Ticket size={13} /> {decision.coupon_code} saves {money(decision.coupon_discount_paise)}</span>}<span className="rounded-full bg-white/[.06] px-3 py-1.5 text-xs text-slate-300">Margin after discount: {decision.margin_after_discount_percent ?? "Needs review"}{decision.margin_after_discount_percent !== null && "%"}</span><span className={`rounded-full border px-3 py-1.5 text-xs font-medium ${riskStyle}`}>Risk {decision.risk_level}</span></div><section className="mt-6 border-t border-white/10 pt-5"><p className="text-sm font-medium">Why this was selected</p><ul className="mt-3 space-y-2">{decision.why_selected.map((reason) => <li key={reason} className="flex gap-2 text-sm leading-5 text-slate-400"><CheckCircle2 className="mt-0.5 shrink-0 text-emerald-300" size={15} />{reason}</li>)}</ul></section><section className="mt-6 rounded-xl border border-amber-400/15 bg-amber-400/[.06] p-4"><p className="flex items-center gap-2 text-sm font-medium text-amber-100"><CircleAlert size={16} /> Why alternatives are not admitted</p><ul className="mt-3 space-y-2">{decision.why_rejected.map((reason) => <li key={reason} className="text-sm leading-5 text-slate-400">• {reason}</li>)}</ul></section></article><article className="rounded-2xl border border-white/10 bg-slate-900/80 p-6"><div className="flex items-start gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-cyan-400/10 text-cyan-200"><BarChart3 size={20} /></div><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-cyan-200">Confidence calculation</p><h3 className="mt-1 text-xl font-semibold">Transparent, rule-derived score</h3></div></div><p className="mt-4 text-sm leading-6 text-slate-400">{decision.confidence_formula}</p><div className="mt-6 space-y-5">{decision.confidence_components.map((component) => <div key={component.label}><div className="flex items-center justify-between gap-3 text-sm"><span className="font-medium">{component.label} <span className="text-slate-500">· {component.weight}% weight</span></span><span className="text-cyan-200">{component.score}%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-gradient-to-r from-violet-400 to-cyan-300" style={{ width: `${component.score}%` }} /></div><p className="mt-1.5 text-xs leading-5 text-slate-500">{component.detail}</p></div>)}</div><section className="mt-7 border-t border-white/10 pt-5"><p className="text-sm font-medium">Expected business impact</p><div className="mt-3 grid grid-cols-2 gap-3"><Metric label="Incremental orders" value={`${decision.expected_incremental_orders}/month`} /><Metric label="Est. revenue lift" value={`₹${decision.expected_monthly_uplift_inr.toLocaleString("en-IN")}/month`} /></div><p className="mt-3 text-xs leading-5 text-slate-500">Assumptions: {decision.assumptions.join(" · ") || "Merchant review required."}</p></section></article></section><section className="grid gap-6 xl:grid-cols-[1.12fr_.88fr]"><DecisionTrace decision={decision} /><GovernanceCard decision={decision} /></section><SimulationPanel simulation={simulation} running={running} /></div>;
}

function HeaderStat({ icon, label, value, tone }: { icon: React.ReactNode; label: string; value: string; tone: "violet" | "cyan" | "emerald" | "amber" }) { const toneClass = { violet: "text-violet-200", cyan: "text-cyan-200", emerald: "text-emerald-300", amber: "text-amber-200" }[tone]; return <div className="rounded-xl border border-white/10 bg-slate-950/40 p-4"><div className={`flex items-center gap-2 text-xs font-medium ${toneClass}`}>{icon}{label}</div><p className="mt-2 text-lg font-semibold capitalize">{value}</p></div>; }
function Metric({ label, value }: { label: string; value: string }) { return <div><p className="text-[11px] uppercase tracking-wide text-slate-500">{label}</p><p className="mt-1 text-sm font-semibold text-slate-100">{value}</p></div>; }

function DecisionTrace({ decision }: { decision: DecisionCenter }) { return <article className="rounded-2xl border border-white/10 bg-slate-900/80 p-6"><div className="flex items-start gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-violet-500/15 text-violet-200"><Activity size={20} /></div><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-violet-200">Agent reasoning timeline</p><h3 className="mt-1 text-xl font-semibold">Decision trace</h3></div></div><div className="mt-6 space-y-0">{decision.decision_trace.map((event, index) => <div key={`${event.step}-${event.title}`} className="grid grid-cols-[32px_1fr] gap-3"><div className="flex flex-col items-center"><div className={`grid h-8 w-8 place-items-center rounded-full text-xs font-semibold ${event.status === "awaiting_approval" ? "bg-amber-400/15 text-amber-200" : "bg-violet-500/15 text-violet-200"}`}>{event.step}</div>{index !== decision.decision_trace.length - 1 && <div className="h-full min-h-7 w-px bg-white/10" />}</div><div className="pb-5"><div className="flex flex-wrap items-center gap-2"><p className="text-sm font-medium">{event.title}</p><span className="rounded-full bg-white/[.06] px-2 py-0.5 text-[11px] text-slate-400">{event.actor}</span></div><p className="mt-1 text-sm leading-5 text-slate-400">{event.detail}</p></div></div>)}</div></article>; }

function GovernanceCard({ decision }: { decision: DecisionCenter }) { return <article className="rounded-2xl border border-white/10 bg-slate-900/80 p-6"><div className="flex items-start gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-emerald-400/10 text-emerald-300"><ShieldCheck size={20} /></div><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-emerald-300">Governed AI</p><h3 className="mt-1 text-xl font-semibold">Commercial controls</h3></div></div><div className="mt-6 space-y-3">{decision.guardrails.map((guardrail) => <div key={guardrail.name} className="rounded-xl border border-white/10 bg-white/[.03] p-4"><div className="flex items-center justify-between gap-3"><p className="text-sm font-medium">{guardrail.name}</p><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${guardrail.status === "passed" ? "bg-emerald-400/10 text-emerald-300" : guardrail.status === "blocked" ? "bg-rose-400/10 text-rose-200" : "bg-amber-400/10 text-amber-200"}`}>{guardrail.status}</span></div><p className="mt-2 text-sm leading-5 text-slate-400">{guardrail.detail}</p></div>)}</div><section className="mt-6 rounded-xl border border-rose-400/15 bg-rose-400/[.05] p-4"><p className="text-sm font-medium text-rose-100">Risk explanation</p><ul className="mt-2 space-y-2">{decision.risk_reasons.map((reason) => <li key={reason} className="text-sm leading-5 text-slate-400">• {reason}</li>)}</ul></section><section className="mt-6 border-t border-white/10 pt-5"><p className="flex items-center gap-2 text-sm font-medium"><ClipboardCheck size={16} className="text-cyan-200" /> Evidence and audit references</p><div className="mt-3 flex flex-wrap gap-2">{decision.evidence_sources.map((source) => <span key={source} className="rounded-md bg-white/[.05] px-2.5 py-1.5 text-xs text-slate-400">{source}</span>)}</div><div className="mt-4 space-y-2">{decision.audit_references.map((reference) => <p key={reference.id} className="text-xs leading-5 text-slate-500"><span className="font-medium text-cyan-200">AUD-{reference.id}</span> · {reference.event_type.replace(/_/g, " ")}</p>)}</div></section></article>; }

function SimulationPanel({ simulation, running }: { simulation: CommerceSimulation | null; running: boolean }) { const [visible, setVisible] = useState(0); useEffect(() => { if (!simulation) return; setVisible(0); const timer = window.setInterval(() => setVisible((current) => current >= simulation.events.length ? current : current + 1), 600); return () => window.clearInterval(timer); }, [simulation]); if (!simulation && !running) return null; const shown = simulation?.events.slice(0, visible) ?? []; return <section className="rounded-2xl border border-cyan-400/25 bg-gradient-to-br from-cyan-400/[.10] via-slate-900 to-slate-950 p-6 sm:p-7"><div className="flex items-start gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-cyan-400/15 text-cyan-200"><Sparkles size={20} /></div><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-cyan-200">Commerce simulation</p><h3 className="mt-1 text-xl font-semibold">{running ? "Preparing safe demo run…" : "End-to-end governed commerce loop"}</h3><p className="mt-1 text-sm text-slate-400">{simulation?.safety_notice ?? "No live payment action is being performed."}</p></div></div><div className="mt-6 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{shown.map((event) => <article key={event.step} className="animate-in fade-in slide-in-from-bottom-2 rounded-xl border border-white/10 bg-slate-950/50 p-4"><div className="flex items-center justify-between gap-3"><p className="text-sm font-medium">{event.step}. {event.title}</p><span className={`rounded-full px-2 py-0.5 text-[10px] font-medium ${event.status === "simulated" ? "bg-cyan-400/10 text-cyan-200" : event.status === "completed" ? "bg-emerald-400/10 text-emerald-300" : "bg-white/[.06] text-slate-400"}`}>{event.status.replace("_", " ")}</span></div><p className="mt-2 text-xs leading-5 text-slate-400">{event.detail}</p></article>)}</div>{simulation && visible >= simulation.events.length && <p className="mt-5 text-xs text-slate-500">Simulation {simulation.simulation_id} · Audit references {simulation.audit_references.map((reference) => `AUD-${reference.id}`).join(", ")}</p>}</section>; }
