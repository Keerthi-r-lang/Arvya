import { Activity, BadgeCheck, BarChart3, Bot, CheckCircle2, CircleAlert, ClipboardCheck, Play, ShieldCheck, Sparkles, Ticket, TrendingUp } from "lucide-react";
import { useEffect, useState } from "react";
import type { CommerceSimulation, DecisionCenter } from "../types";

const money = (paise: number) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(paise / 100);

export function DecisionCenterPage({ decision, simulation, loading, running, error, onRunSimulation }: { decision: DecisionCenter | null; simulation: CommerceSimulation | null; loading: boolean; running: boolean; error: string | null; onRunSimulation: () => Promise<void> }) {
  if (loading) return <section className="grid min-h-[55vh] place-items-center"><p className="text-sm text-slate-500">Preparing governed decision trace…</p></section>;
  if (!decision) return <section className="grid min-h-[55vh] place-items-center rounded-2xl border border-dashed border-sky-200 bg-white p-8 text-center shadow-sm"><div><Sparkles className="mx-auto text-sky-600" /><h2 className="mt-4 text-xl font-semibold text-slate-900">No governed decision is ready yet</h2><p className="mt-2 max-w-md text-sm leading-6 text-slate-500">Generate a growth opportunity, then return here to inspect its confidence, evidence, guardrails, and audit trace.</p></div></section>;
  const riskStyle = decision.risk_level === "low" ? "border-emerald-200 bg-emerald-50 text-emerald-700" : decision.risk_level === "medium" ? "border-amber-200 bg-amber-50 text-amber-700" : "border-rose-200 bg-rose-50 text-rose-700";
  return <div className="space-y-7 text-slate-900"><section className="relative overflow-hidden rounded-2xl border border-sky-200 bg-gradient-to-br from-sky-50 via-white to-indigo-50 p-7 shadow-sm sm:p-9"><div className="absolute -right-20 -top-20 h-64 w-64 rounded-full bg-sky-200/35 blur-3xl" /><div className="relative flex flex-wrap items-end justify-between gap-5"><div><p className="text-sm font-semibold uppercase tracking-[.18em] text-sky-700">AI Decision Center</p><h2 className="mt-2 max-w-3xl text-3xl font-semibold tracking-tight text-slate-950">One governed decision. Every reason visible.</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-600">Arvya combines catalog evidence, commercial guardrails, merchant approval and Razorpay-safe execution. AI explains the decision; rules protect the money.</p></div><button onClick={() => void onRunSimulation()} disabled={running} className="inline-flex items-center gap-2 rounded-lg bg-sky-600 px-4 py-3 text-sm font-semibold text-white shadow-sm hover:bg-sky-700 disabled:opacity-60"><Play size={16} fill="currentColor" /> {running ? "Running safe simulation…" : "Run Full Commerce Simulation"}</button></div><div className="relative mt-7 grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><HeaderStat icon={<BadgeCheck size={18} />} label="Approval" value={decision.approval_status.replace("_", " ")} tone="sky" /><HeaderStat icon={<Activity size={18} />} label="Decision confidence" value={`${decision.confidence_score}%`} tone="indigo" /><HeaderStat icon={<ShieldCheck size={18} />} label="Risk score" value={`${decision.risk_score}/100 · ${decision.risk_level}`} tone="emerald" /><HeaderStat icon={<TrendingUp size={18} />} label="Expected monthly lift" value={`₹${decision.expected_monthly_uplift_inr.toLocaleString("en-IN")}`} tone="amber" /></div></section>{error && <div className="rounded-xl border border-rose-200 bg-rose-50 p-4 text-sm text-rose-700">{error}</div>}<section className="grid gap-6 xl:grid-cols-[1.12fr_.88fr]"><article className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-start gap-3"><IconFrame tone="sky"><Bot size={20} /></IconFrame><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-sky-700">Selected recommendation</p><h3 className="mt-1 text-xl font-semibold text-slate-950">{decision.title}</h3><p className="mt-1 text-sm text-slate-500">{decision.recommendation_type} · merchant {decision.approval_status.replace("_", " ")}</p></div></div><div className="mt-6 grid gap-3 rounded-xl border border-slate-200 bg-slate-50 p-4 sm:grid-cols-3"><Metric label="Original total" value={money(decision.original_price_paise)} /><Metric label="Merchant offer" value={money(decision.offer_price_paise)} /><Metric label="Final payable" value={money(decision.final_price_paise)} /></div><div className="mt-4 flex flex-wrap gap-2">{decision.coupon_code && <span className="inline-flex items-center gap-1 rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-medium text-emerald-700"><Ticket size={13} /> {decision.coupon_code} saves {money(decision.coupon_discount_paise)}</span>}<span className="rounded-full bg-slate-100 px-3 py-1.5 text-xs text-slate-600">Margin after discount: {decision.margin_after_discount_percent ?? "Needs review"}{decision.margin_after_discount_percent !== null && "%"}</span><span className={`rounded-full border px-3 py-1.5 text-xs font-medium ${riskStyle}`}>Risk {decision.risk_level}</span></div><section className="mt-6 border-t border-slate-100 pt-5"><p className="text-sm font-semibold text-slate-900">Why this was selected</p><ul className="mt-3 space-y-2">{decision.why_selected.map((reason) => <li key={reason} className="flex gap-2 text-sm leading-5 text-slate-600"><CheckCircle2 className="mt-0.5 shrink-0 text-emerald-600" size={15} />{reason}</li>)}</ul></section><section className="mt-6 rounded-xl border border-amber-200 bg-amber-50 p-4"><p className="flex items-center gap-2 text-sm font-semibold text-amber-900"><CircleAlert size={16} /> Why alternatives are not admitted</p><ul className="mt-3 space-y-2">{decision.why_rejected.map((reason) => <li key={reason} className="text-sm leading-5 text-amber-800">• {reason}</li>)}</ul></section></article><article className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-start gap-3"><IconFrame tone="indigo"><BarChart3 size={20} /></IconFrame><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-indigo-700">Confidence calculation</p><h3 className="mt-1 text-xl font-semibold text-slate-950">Transparent, rule-derived score</h3></div></div><p className="mt-4 text-sm leading-6 text-slate-600">{decision.confidence_formula}</p><div className="mt-6 space-y-5">{decision.confidence_components.map((component) => <div key={component.label}><div className="flex items-center justify-between gap-3 text-sm"><span className="font-medium text-slate-800">{component.label} <span className="text-slate-400">· {component.weight}% weight</span></span><span className="text-sky-700">{component.score}%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-gradient-to-r from-sky-500 to-indigo-500" style={{ width: `${component.score}%` }} /></div><p className="mt-1.5 text-xs leading-5 text-slate-500">{component.detail}</p></div>)}</div><section className="mt-7 border-t border-slate-100 pt-5"><p className="text-sm font-semibold text-slate-900">Expected business impact</p><div className="mt-3 grid grid-cols-2 gap-3"><Metric label="Incremental orders" value={`${decision.expected_incremental_orders}/month`} /><Metric label="Est. revenue lift" value={`₹${decision.expected_monthly_uplift_inr.toLocaleString("en-IN")}/month`} /></div><p className="mt-3 text-xs leading-5 text-slate-500">Assumptions: {decision.assumptions.join(" · ") || "Merchant review required."}</p></section></article></section><section className="grid gap-6 xl:grid-cols-[1.12fr_.88fr]"><DecisionTrace decision={decision} /><GovernanceCard decision={decision} /></section><SimulationPanel simulation={simulation} running={running} /></div>;
}

function IconFrame({ tone, children }: { tone: "sky" | "indigo" | "emerald"; children: React.ReactNode }) { const colors = { sky: "bg-sky-50 text-sky-700", indigo: "bg-indigo-50 text-indigo-700", emerald: "bg-emerald-50 text-emerald-700" }[tone]; return <div className={`grid h-10 w-10 place-items-center rounded-xl ${colors}`}>{children}</div>; }
function HeaderStat({ icon, label, value, tone }: { icon: React.ReactNode; label: string; value: string; tone: "sky" | "indigo" | "emerald" | "amber" }) { const toneClass = { sky: "text-sky-700", indigo: "text-indigo-700", emerald: "text-emerald-700", amber: "text-amber-700" }[tone]; return <div className="rounded-xl border border-slate-200 bg-white/90 p-4 shadow-sm"><div className={`flex items-center gap-2 text-xs font-semibold ${toneClass}`}>{icon}{label}</div><p className="mt-2 text-lg font-semibold capitalize text-slate-900">{value}</p></div>; }
function Metric({ label, value }: { label: string; value: string }) { return <div><p className="text-[11px] uppercase tracking-wide text-slate-500">{label}</p><p className="mt-1 text-sm font-semibold text-slate-900">{value}</p></div>; }

function DecisionTrace({ decision }: { decision: DecisionCenter }) { return <article className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-start gap-3"><IconFrame tone="sky"><Activity size={20} /></IconFrame><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-sky-700">Agent reasoning timeline</p><h3 className="mt-1 text-xl font-semibold text-slate-950">Decision trace</h3></div></div><div className="mt-6 space-y-0">{decision.decision_trace.map((event, index) => <div key={`${event.step}-${event.title}`} className="grid grid-cols-[32px_1fr] gap-3"><div className="flex flex-col items-center"><div className={`grid h-8 w-8 place-items-center rounded-full text-xs font-semibold ${event.status === "awaiting_approval" ? "bg-amber-100 text-amber-700" : "bg-sky-100 text-sky-700"}`}>{event.step}</div>{index !== decision.decision_trace.length - 1 && <div className="h-full min-h-7 w-px bg-slate-200" />}</div><div className="pb-5"><div className="flex flex-wrap items-center gap-2"><p className="text-sm font-semibold text-slate-800">{event.title}</p><span className="rounded-full bg-slate-100 px-2 py-0.5 text-[11px] text-slate-500">{event.actor}</span></div><p className="mt-1 text-sm leading-5 text-slate-600">{event.detail}</p></div></div>)}</div></article>; }

function GovernanceCard({ decision }: { decision: DecisionCenter }) { return <article className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-start gap-3"><IconFrame tone="emerald"><ShieldCheck size={20} /></IconFrame><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-emerald-700">Governed AI</p><h3 className="mt-1 text-xl font-semibold text-slate-950">Commercial controls</h3></div></div><div className="mt-6 space-y-3">{decision.guardrails.map((guardrail) => <div key={guardrail.name} className="rounded-xl border border-slate-200 bg-slate-50 p-4"><div className="flex items-center justify-between gap-3"><p className="text-sm font-semibold text-slate-800">{guardrail.name}</p><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${guardrail.status === "passed" ? "bg-emerald-100 text-emerald-700" : guardrail.status === "blocked" ? "bg-rose-100 text-rose-700" : "bg-amber-100 text-amber-700"}`}>{guardrail.status}</span></div><p className="mt-2 text-sm leading-5 text-slate-600">{guardrail.detail}</p></div>)}</div><section className="mt-6 rounded-xl border border-rose-200 bg-rose-50 p-4"><p className="text-sm font-semibold text-rose-900">Risk explanation</p><ul className="mt-2 space-y-2">{decision.risk_reasons.map((reason) => <li key={reason} className="text-sm leading-5 text-rose-800">• {reason}</li>)}</ul></section><section className="mt-6 border-t border-slate-100 pt-5"><p className="flex items-center gap-2 text-sm font-semibold text-slate-900"><ClipboardCheck size={16} className="text-sky-700" /> Evidence and audit references</p><div className="mt-3 flex flex-wrap gap-2">{decision.evidence_sources.map((source) => <span key={source} className="rounded-md bg-sky-50 px-2.5 py-1.5 text-xs text-sky-800">{source}</span>)}</div><div className="mt-4 space-y-2">{decision.audit_references.map((reference) => <p key={reference.id} className="text-xs leading-5 text-slate-500"><span className="font-semibold text-sky-700">AUD-{reference.id}</span> · {reference.event_type.replace(/_/g, " ")}</p>)}</div></section></article>; }

function SimulationPanel({ simulation, running }: { simulation: CommerceSimulation | null; running: boolean }) { const [visible, setVisible] = useState(0); useEffect(() => { if (!simulation) return; setVisible(0); const timer = window.setInterval(() => setVisible((current) => current >= simulation.events.length ? current : current + 1), 600); return () => window.clearInterval(timer); }, [simulation]); if (!simulation && !running) return null; const shown = simulation?.events.slice(0, visible) ?? []; return <section className="rounded-2xl border border-sky-200 bg-gradient-to-br from-sky-50 via-white to-indigo-50 p-6 shadow-sm sm:p-7"><div className="flex items-start gap-3"><IconFrame tone="sky"><Sparkles size={20} /></IconFrame><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-sky-700">Commerce simulation</p><h3 className="mt-1 text-xl font-semibold text-slate-950">{running ? "Preparing safe demo run…" : "End-to-end governed commerce loop"}</h3><p className="mt-1 text-sm text-slate-600">{simulation?.safety_notice ?? "No live payment action is being performed."}</p></div></div><div className="mt-6 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{shown.map((event) => <article key={event.step} className="animate-in fade-in slide-in-from-bottom-2 rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-center justify-between gap-3"><p className="text-sm font-semibold text-slate-800">{event.step}. {event.title}</p><span className={`rounded-full px-2 py-0.5 text-[10px] font-medium ${event.status === "simulated" ? "bg-sky-100 text-sky-700" : event.status === "completed" ? "bg-emerald-100 text-emerald-700" : "bg-slate-100 text-slate-500"}`}>{event.status.replace("_", " ")}</span></div><p className="mt-2 text-xs leading-5 text-slate-600">{event.detail}</p></article>)}</div>{simulation && visible >= simulation.events.length && <p className="mt-5 text-xs text-slate-500">Simulation {simulation.simulation_id} · Audit references {simulation.audit_references.map((reference) => `AUD-${reference.id}`).join(", ")}</p>}</section>; }
