import { Bot, CheckCircle2, CircleAlert, ShieldCheck } from "lucide-react";
import type { AuditLog } from "../types";

export function AuditTrailPage({ events }: { events: AuditLog[] }) { return <div className="space-y-7"><section><p className="text-sm font-semibold uppercase tracking-[.18em] text-sky-600">Trust center</p><h2 className="mt-2 text-3xl font-semibold tracking-tight">Auditable agentic commerce</h2><p className="mt-2 max-w-2xl text-slate-500">Every agent, merchant, customer, and Razorpay action is recorded with an actor, entity, and timestamp.</p></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-3 border-b border-slate-100 pb-4"><div className="grid h-9 w-9 place-items-center rounded-lg bg-sky-50 text-sky-600"><ShieldCheck size={19} /></div><div><h3 className="font-semibold">Immutable activity ledger</h3><p className="text-xs text-slate-500">Append-only audit records for merchant decisions and payment operations.</p></div></div><div className="mt-5 space-y-5">{events.length ? events.map((event) => <article key={event.id} className="grid grid-cols-[32px_1fr] gap-3"><div className={`grid h-8 w-8 place-items-center rounded-full ${event.actor_type === "agent" ? "bg-violet-50 text-violet-600" : event.actor_type === "razorpay" ? "bg-emerald-50 text-emerald-600" : "bg-sky-50 text-sky-600"}`}>{event.actor_type === "agent" ? <Bot size={16} /> : event.event_type.includes("failed") ? <CircleAlert size={16} /> : <CheckCircle2 size={16} />}</div><div className="border-b border-slate-100 pb-5"><div className="flex flex-wrap items-center gap-x-2 gap-y-1"><p className="text-sm font-medium text-slate-900">{humanize(event.event_type)}</p><span className="text-xs text-slate-400">{event.actor_type} · {event.entity_type} #{event.entity_id}</span></div><p className="mt-1 text-sm leading-6 text-slate-600">{event.detail}</p><p className="mt-1 text-xs text-slate-400">{new Date(event.created_at).toLocaleString()}</p></div></article>) : <p className="py-8 text-center text-sm text-slate-500">No audit events yet.</p>}</div></section></div>; }

function humanize(value: string) { return value.replace(/_/g, " ").replace(/\b\w/g, (letter: string) => letter.toUpperCase()); }
