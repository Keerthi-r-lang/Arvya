import { Boxes, CircleAlert, IndianRupee, Layers3, Sparkles } from "lucide-react";
import type { AuditLog, CatalogSummary, CommerceMetrics, Merchant } from "../types";
import { MetricCard } from "../components/dashboard/MetricCard";

const formatINR = (paise: number) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(paise / 100);

export function DashboardPage({ merchant, summary, metrics, activity, onOpenCatalog, onGenerate, generating }: { merchant: Merchant; summary: CatalogSummary; metrics: CommerceMetrics; activity: AuditLog[]; onOpenCatalog: () => void; onGenerate: () => Promise<void>; generating: boolean }) {
  return <div className="space-y-8"><section className="rounded-2xl border border-sky-200 bg-white p-7 shadow-sm sm:p-9"><p className="text-sm font-medium text-sky-600">Merchant intelligence workspace</p><h2 className="mt-2 text-3xl font-semibold tracking-tight">Grow revenue with governed AI offers.</h2><p className="mt-3 max-w-2xl text-slate-500">{merchant.name} can generate explainable offers, approve every commercial decision, and track verified payment outcomes.</p><div className="mt-6 flex flex-wrap gap-3"><button onClick={() => void onGenerate()} disabled={generating} className="rounded-lg bg-sky-600 px-4 py-2 text-sm font-semibold text-white hover:bg-sky-700 disabled:opacity-60">{generating ? "Analyzing catalog…" : "Generate growth opportunities"}</button><button onClick={onOpenCatalog} className="rounded-lg border border-slate-200 bg-white px-4 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50">Review catalog</button></div></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Actual paid revenue" value={formatINR(metrics.actual_paid_revenue_paise)} detail={`${metrics.paid_links} verified payments`} icon={<IndianRupee size={19} />} /><MetricCard label="Payment links" value={metrics.payment_links_created} detail="Customer checkout attempts" icon={<Layers3 size={19} />} /><MetricCard label="Approved offers" value={metrics.approved_offers} detail={`₹${metrics.estimated_monthly_uplift_inr.toLocaleString("en-IN")} estimated monthly uplift`} icon={<Sparkles size={19} />} /><MetricCard label="Catalog products" value={summary.total_products} detail={`${summary.active_products} active · ${summary.categories} categories`} icon={<Boxes size={19} />} /></section><section className="grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><article className="rounded-2xl border border-white/10 bg-slate-900/80 p-6"><div className="flex items-start justify-between"><div><h2 className="font-semibold">Growth Agent</h2><p className="mt-1 text-sm text-slate-400">Governed multi-agent workflow</p></div><Sparkles className="text-sky-500" /></div><div className="mt-7 rounded-xl border border-dashed border-sky-200 bg-sky-50 p-5"><p className="font-medium">AI recommends; the merchant decides.</p><p className="mt-2 text-sm leading-6 text-slate-500">Catalog, bundle, upsell, campaign, and impact agents record evidence before a recommendation enters the approval queue.</p><button onClick={() => void onGenerate()} disabled={generating} className="mt-5 rounded-lg bg-sky-600 px-4 py-2 text-sm font-medium text-white hover:bg-sky-700 disabled:opacity-60">{generating ? "Running…" : "Run Growth Agent"}</button></div></article><article className="rounded-2xl border border-white/10 bg-slate-900/80 p-6"><h2 className="font-semibold">Recent activity</h2><div className="mt-4 space-y-4">{activity.length ? activity.map((item) => <div key={item.id} className="border-l border-sky-300 pl-4"><p className="text-sm text-slate-200">{item.detail}</p><p className="mt-1 text-xs text-slate-500">{new Date(item.created_at).toLocaleString()}</p></div>) : <p className="text-sm text-slate-500">No activity yet.</p>}</div></article></section></div>;
}
